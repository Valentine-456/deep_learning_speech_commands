model:
  type: cnn
  channels: [32, 64, 128, 256]
  dropout: 0.1

data:
  cache_dir: data/cache
  feature: mel          # mel | mfcc
  augmentation: true

training:
  epochs: 40
  batch_size: 64
  lr: 0.001
  workers: 4
  seed: 42
  early_stopping_patience: 7
  out_dir: results
